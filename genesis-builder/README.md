# Genesis Builder (KCT)
Erzeugt aus `configs/token-spec.yaml` + `configs/genesis_allocation.csv` eine `genesis.json`.

## Run
```powershell
cd genesis-builder
cargo run --release
```
Ergebnis: `genesis.json` im Ordner.
